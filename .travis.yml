language: generic
sudo: required
services: 
 - docker

before_install: 
 - docker build -t jtangs/docker-react -f Dockerfile.dev .

script: 
 - docker run -e CI=true jtangs/docker-react npm run test

